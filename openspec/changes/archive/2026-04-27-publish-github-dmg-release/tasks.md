## 1. Release Packaging

- [x] 1.1 Implement GitHub release provides installable macOS assets in `scripts/package_release.sh` so each version produces both `.zip` and `.dmg` outputs.
- [x] 1.2 Add any supporting release packaging paths or file handling needed for repeatable `.dmg` generation.

## 2. GitHub Release Automation

- [x] 2.1 Implement GitHub release page includes Traditional Chinese release content with a tag-driven GitHub Actions workflow.
- [x] 2.2 Implement GitHub release page displays screenshot context by validating release note input and uploading assets with the release workflow.

## 3. Download Guidance

- [x] 3.1 Implement Repository front page links users to stable downloads in `README.md` with `.dmg` download guidance.
- [x] 3.2 Rewrite current release note documents in Traditional Chinese and keep screenshot references on stable repository paths.
